import Foundation

struct Constants {
    struct Storyboard {
        static let homeViewController = "BoardVC"
    }
    
    struct Video {
        static let videoName = "Kanban"
        static let videoType = "mp4"
    }
}
